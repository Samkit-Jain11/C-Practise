#include <stdio.h>

int main() {
    int a , b ;

    printf("Enter value of a: ");
    scanf("%d", &a);

    printf("Enter value of b: ");
    scanf("%d", &b);

    printf("After swapping, value of a becomes %d" , b );
    printf(" value of b becomes %d", a);

    return 0;
}
